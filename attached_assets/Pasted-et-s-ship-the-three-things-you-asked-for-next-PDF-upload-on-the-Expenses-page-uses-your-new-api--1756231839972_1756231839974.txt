et’s ship the three things you asked for next:

PDF upload on the Expenses page (uses your new /api/bank-imports/pdf)

Drinks in Purchase Tally = itemized (Coke 6, Fanta 12), not a single number

Mobile/tablet polish (sidebar collapsed by default + font/spacing tweaks)

Below I give you copy-paste patches. If a path differs slightly in your repo, search by the filename and drop in the code.

1) Enable PDF upload in the Bank Statement card

File: client/src/pages/expenses/BankUploadCard.tsx
(If yours is named differently, search for the component that renders the “Bank Statement Upload” tile.)

Replace the file with this:

import React, { useState, useRef } from "react";

type Props = {
  onImported?: () => void;
};

export default function BankUploadCard({ onImported }: Props) {
  const [bank, setBank] = useState<"generic">("generic");
  const [busy, setBusy] = useState(false);
  const [msg, setMsg] = useState<string | null>(null);
  const fileInput = useRef<HTMLInputElement | null>(null);

  const handleDrop = async (e: React.DragEvent) => {
    e.preventDefault();
    if (busy) return;
    const f = e.dataTransfer.files?.[0];
    if (f) await sendFile(f);
  };

  const sendFile = async (file: File) => {
    setBusy(true);
    setMsg(null);
    try {
      const fd = new FormData();
      fd.append("file", file);

      // CSV -> /csv, PDF -> /pdf
      const isPDF =
        file.type === "application/pdf" || file.name.toLowerCase().endsWith(".pdf");
      const url = isPDF
        ? "/api/bank-imports/pdf"
        : "/api/bank-imports/csv";

      const res = await fetch(url, {
        method: "POST",
        body: fd,
      });

      if (!res.ok) {
        const text = await res.text();
        throw new Error(text || `Upload failed: ${res.status}`);
      }
      setMsg("Upload queued. Review & approve in the list below.");
      onImported?.();
    } catch (err: any) {
      setMsg(err.message || "Upload failed");
    } finally {
      setBusy(false);
    }
  };

  const onSelectFile = async (e: React.ChangeEvent<HTMLInputElement>) => {
    const f = e.target.files?.[0];
    if (f) await sendFile(f);
  };

  return (
    <div className="rounded-xl border border-gray-200 bg-white p-5">
      <div className="flex items-center justify-between mb-3">
        <div>
          <h3 className="text-xl font-semibold">Bank Statement Upload</h3>
          <p className="text-sm text-gray-500">
            Upload CSV or PDF statements to create expense entries. Approve / Edit / Delete after import.
          </p>
        </div>
      </div>

      <div className="mb-4">
        <label className="block text-xs font-medium text-gray-500 mb-1">Bank Source</label>
        <select
          className="w-full rounded-lg border border-gray-300 px-3 py-2"
          value={bank}
          onChange={() => setBank("generic")}
        >
          <option value="generic">Generic CSV / PDF</option>
        </select>
      </div>

      <div
        onDrop={handleDrop}
        onDragOver={(e) => e.preventDefault()}
        className={`border-2 border-dashed rounded-xl p-8 text-center ${
          busy ? "opacity-60" : ""
        }`}
      >
        <div className="text-4xl mb-2">📄</div>
        <div className="text-gray-700 font-medium">
          Drag & drop a <b>CSV or PDF</b> here,
        </div>
        <div className="text-gray-500 mb-3">or click to select</div>

        <button
          className="inline-flex items-center rounded-lg bg-black text-white px-4 py-2 text-sm"
          onClick={() => fileInput.current?.click()}
          disabled={busy}
        >
          {busy ? "Uploading…" : "Choose file"}
        </button>

        <input
          ref={fileInput}
          type="file"
          accept=".csv,application/pdf"
          className="hidden"
          onChange={onSelectFile}
        />
      </div>

      {msg && (
        <div className="mt-3 text-sm text-gray-700">
          {msg}
        </div>
      )}

      <p className="text-xs text-gray-400 mt-4">
        Expected CSV columns (Generic): <b>Date, Description, Amount (THB)</b>.
        PDFs will be parsed automatically (beta).
      </p>
    </div>
  );
}


This sends CSV to /api/bank-imports/csv and PDF to /api/bank-imports/pdf (the route you already have). The card shows a simple status and is cache-safe.

2) Purchase Tally itemized drinks

No schema changes now — we’ll store the detail as structured JSON inside the notes field (prefix DRINKS:). That keeps it deploy-safe today and we can move to a column later without data loss.

2a) Update the Add Purchase modal UI

File: client/src/pages/expenses/PurchaseTallyModal.tsx
(If your file is named differently, search for the “Add Purchase Tally” dialog.)

Replace the Drinks area with an itemized list editor:

// add at top
type DrinkLine = { name: string; qty: number };

const [drinks, setDrinks] = useState<DrinkLine[]>([{ name: "", qty: 0 }]);

const addDrink = () => setDrinks((d) => [...d, { name: "", qty: 0 }]);
const removeDrink = (idx: number) =>
  setDrinks((d) => d.filter((_, i) => i !== idx));
const setDrink = (idx: number, key: keyof DrinkLine, val: string | number) =>
  setDrinks((d) => d.map((r, i) => (i === idx ? { ...r, [key]: val } : r)));


Replace the existing single “Drinks (pcs)” input with:

<div className="mt-4">
  <label className="block text-sm font-medium mb-2">
    Drinks (itemized)
  </label>

  {drinks.map((row, i) => (
    <div key={i} className="flex gap-2 mb-2">
      <input
        className="flex-1 rounded-lg border border-gray-300 px-3 py-2"
        placeholder="e.g., Coke"
        value={row.name}
        onChange={(e) => setDrink(i, "name", e.target.value)}
      />
      <input
        type="number"
        className="w-24 rounded-lg border border-gray-300 px-3 py-2"
        value={row.qty}
        onChange={(e) => setDrink(i, "qty", Number(e.target.value || 0))}
        min={0}
      />
      <button
        type="button"
        className="px-3 rounded-lg border text-sm"
        onClick={() => removeDrink(i)}
      >
        ✕
      </button>
    </div>
  ))}

  <button type="button" className="text-sm text-black underline" onClick={addDrink}>
    + Add drink
  </button>
</div>

2b) When saving, embed the JSON into notes

Find where the form payload is built (the object you POST to /api/purchase-tally). Add this before sending:

// keep the existing notes, but append a structured block with DRINKS
const clean = drinks.filter(d => d.name && d.qty > 0);
const drinksTag = clean.length ? `\nDRINKS:${JSON.stringify(clean)}` : "";
payload.notes = (payload.notes || "") + drinksTag;

// keep any legacy "drinks" integer if your API requires it
payload.drinks = clean.reduce((sum, d) => sum + d.qty, 0);

2c) Show itemized drinks in the list & summary

Where you render each tally row and the MTD summary, parse back the itemization:

function parseDrinkItems(notes?: string | null): { name: string; qty: number }[] {
  if (!notes) return [];
  const tag = "DRINKS:";
  const at = notes.indexOf(tag);
  if (at < 0) return [];
  try {
    const json = notes.substring(at + tag.length).trim();
    return JSON.parse(json);
  } catch {
    return [];
  }
}

// per-card or per-row
const items = parseDrinkItems(row.notes);
// show chips like: [Coke ×6] [Fanta ×12]


In your MTD Purchase Tally summary card, build a map:

const perDrink = new Map<string, number>();
rows.forEach(r => {
  parseDrinkItems(r.notes).forEach(({name, qty}) => {
    perDrink.set(name, (perDrink.get(name) || 0) + qty);
  });
});
// render Object.entries([...perDrink]) as chips


This gives you accurate per-drink totals now, without any server migration.

3) Mobile & tablet polish
3a) Sidebar collapsed by default on tablet/mobile

Find your sidebar container (often something like <aside className="...">). Replace the width classes with a responsive one:

// before: w-64
// after:
<aside className="w-16 lg:w-64 transition-all duration-200">
  {/* hide labels on small */}
  <span className="hidden lg:inline">Menu Label</span>
</aside>


Anywhere you have nav labels, wrap them with hidden lg:inline so on smaller screens you see only icons.

3b) Font & spacing scale

If you use Tailwind, add these global utilities once:

File: client/src/index.css (or your global CSS)

/* Typography & spacing for small screens */
@media (max-width: 1024px) {
  html { font-size: 15px; }
  .card { padding: 14px; }
}

@media (max-width: 640px) {
  html { font-size: 14px; }
  .card { padding: 12px; }
}


Then ensure your main cards have className="card ...", e.g.:

<div className="card rounded-xl border bg-white"> … </div>


(If you don’t want to rename classes, just add the padding rules to your existing card class.)

What you’ll see after this

Expenses: you can drag a PDF directly onto the upload card; it hits /api/bank-imports/pdf and shows a status.

Purchase Tally: drinks are itemized in the modal; the list and MTD summary show per-drink chips; the old single “drinks” number still works for backwards compatibility.

Mobile: sidebar auto-collapses, text scales down a touch, cards have tighter spacing.

If you want, I can bundle these three edits into a single shell patch next—just say the word and I’ll give you one command that writes these files for you.